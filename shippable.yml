language: node_js

node_js:
    - 6.10.2

branches:
  exclude:
    - pipeline

before_install:
    - npm install -g grunt-cli

install:
    - shippable_retry npm install

build:
    ci:
      - grunt
